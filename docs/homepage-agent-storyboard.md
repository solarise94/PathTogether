# 主页演示脚本：从真实 TCGA 操作记录到画面

2026-09-15。先核对记录，再设计脚本，最后实现。替换此前抽象图、白色视野框和扫描动画。

## 依据

只读核对 homePC sidecar-sessions 中两段 TCGA 操作记录。主样本为已结束的 `sess_f38dfd3facf64f25`，对照样本 `sess_77c4a335d0dd4cce` 尚未结束，不用于宣称已生成报告。只使用工具调用、工具返回、可见回答和快照坐标，不使用内部推理内容。

主样本使用 smokebot 副本，原始公开图为 `TCGA-49-AAR4-01Z-00-DX1.EDB32358-AF23-4F81-A99F-15574A2DE28E.svs`，79680 × 51516，MPP 0.2525，扫描倍率 40×。Demo 目录已有该图，见 scripts/seed_demo_tcga_catalog.py。原图与副本的尺寸及记录中的图像指纹一致。

实际循环是：概览 → goto → snapshot → mark_observation / create_annotation → complete_snapshot_review → 下一区域，最终 finish。并非每个区域必定有标注；本例没有成功的计数/分割插件调用，slide_summary 曾返回降级占位摘要，因此不展示插件分析或计数。工具记录中移动被未完成快照复核阻止的重试不放进短片，保留成功动作顺序。

## 15 秒操作演示脚本（扩充稿）

在原四步节选之上，按 `sess_f38dfd3facf64f25` 的成功动作顺序走完整条读片路径。压缩等待与被守卫拦住的重复 goto，不展示插件分析。总时长约 **15.0 秒**（含镜头运动；最后一卡停住可重播）。显示「真实会话节选 · 已压缩等待时间」。旁白是简化操作反馈，不是逐字引用，也不作为诊断结论。

时间轴从 0.0 起。每段「停留」含进入该视口的运镜（约 0.5–0.7 秒）。倍率用记录中的近似值：概览 0.5×、level2 ≈2×、level1 ≈10×、level0 ≈40×。

| 拍 | 时间 | 时长 | 画面 | 给访客看的操作反馈 | 记录依据 |
| --- | --- | --- | --- | --- | --- |
| 0 | 0.0–1.4 | 1.4 s | 全片 0.5× 概览，三个组织块同时可见 | 已打开切片，先看整张图再选区域。 | events 4：overview snapshot bbox (0,0,79680,51516) |
| 1 | 1.4–3.0 | 1.6 s | 移到左侧大块，约 2× | 已查看低倍快照，记录纤维脂肪背景和一处致密细胞团。 | 5–13：goto(22000,28000,2)、snapshot、mark_observation「左侧大块中倍整体结构」、complete_snapshot_review(no_annotation)。bbox (13807,19807,16386,16386) |
| 2 | 3.0–4.4 | 1.4 s | 同一团块放大到约 10× | 已靠近致密细胞团，细节仍需高倍确认。 | 15–21：goto(25500,30000,1)、snapshot、complete_snapshot_review(no_annotation)。bbox (23452,27952,4096,4096) |
| 3 | 4.4–6.4 | 2.0 s | 落到约 40×，出现第一张待复核标注卡 | 已获取高倍快照，记录一处待人工复核的观察（密集淋巴样细胞聚集）。 | 23–29：goto(25600,29600,0)、snapshot、create_annotation(25600,29100)、complete_snapshot_review(annotated)。bbox (25088,29088,1024,1024) |
| 4 | 6.4–7.8 | 1.4 s | 拉远并平移到右上组织块，约 2× | 已换到下一块组织，低倍以脂肪和纤维为主。 | 31–37：goto(55000,22000,2)、snapshot、mark_observation「右上组织块中倍结构」。bbox (46807,13807,16386,16386) |
| 5 | 7.8–9.4 | 1.6 s | 右上边缘约 10× 再落到 40×；不落标 | 已高倍查看边缘致密区，细胞细节不清，未作明确标注。 | 39–51：goto(61000,28800,1)→goto(60500,27500,0)、snapshot、mark_observation「右上块局部致密粉碎区」、no_annotation。40× bbox (59988,26988,1024,1024) |
| 6 | 9.4–10.8 | 1.4 s | 平移到右下组织块，约 2× | 已查看右下低倍快照，记录上方一条弧形致密带。 | 53–59：goto(65000,42000,2)、snapshot、mark_observation「右下组织块弧形致密带」。bbox (56807,33807,16386,16386) |
| 7 | 10.8–12.2 | 1.4 s | 沿弧形带放大到约 10× | 已移动到目标附近，细节仍需高倍确认。 | 61–65：goto(67000,35500,1)、snapshot、complete_snapshot_review(no_annotation)。bbox (64952,33452,4096,4096) |
| 8 | 12.2–14.2 | 2.0 s | 同一区域约 40×，出现第二张待复核标注卡 | 已获取高倍快照，记录一处待人工复核的观察。 | 67–73：goto(68500,34800,0)、snapshot、create_annotation(68000,34500)、complete_snapshot_review(annotated)。bbox (67988,34288,1024,1024) |
| 9 | 14.2–15.0 | 0.8 s + 停住 | 稍退到邻近 10× 后切到结果卡 | 邻近区域未见新的可定位病灶。本次会话记录两处待复核标注；细节不清的区域未作明确标注。 | 75–81：goto(58200,37300,1)、snapshot、no_annotation；finish 成功。10× bbox (56152,35252,4096,4096) |

结果卡只写操作结果，不写诊断句：两处 pending 标注、若干 no_annotation。画面覆盖会话里三条组织路径，与原先「只节选右下一条」的四步稿不同。

运镜节奏建议：goto 运镜 0.55 s（对数插值），到达后停留至该拍结束。拍 5 把 10× 与 40× 收成一次连续放大，避免 15 秒内停两次。被 pending-review 拦住的重复 goto（记录 11 与 19）不入库。

## 演示文稿（正文四步，最短节选）


| 阶段 | 画面 / 时长 | 给访客看的操作反馈 | 记录依据（messages 从 0 编号） |
| --- | --- | --- | --- |
| 搜索目标区 | 右下组织块低倍实图，停留 1.8 秒 | 已查看低倍快照，记录需要进一步查看的组织带。 | 53–60：goto(65000,42000,2)、snapshot、mark_observation、complete_snapshot_review(no_annotation) |
| 放大目标区 | 平移并放大到 10×，停留 1.6 秒 | 已移动到目标附近，细节仍需高倍确认。 | 61–66：goto(67000,35500,1)、snapshot、complete_snapshot_review(no_annotation) |
| 查看并记录 | 同一区域继续到 40×，停留 2.2 秒 | 已获取高倍快照，记录一处待人工复核的标注。 | 67–74：goto(68500,34800,0)、snapshot、create_annotation、complete_snapshot_review(annotated)，工具返回 pending |
| 整理结果 | 保留高倍图和结果卡，停住 | 本次会话记录两处待复核标注；细节不清的区域未作明确标注。 | 27–29、49–51、71–81，最终 finish 成功。结果覆盖整个会话，画面只节选其中一路径，明确标注这一差别 |

操作反馈是经过简化的记录摘要，不伪装逐字引用或正在运行的实时 Agent。显示「真实会话节选 · 已压缩等待时间」。阶段结束显示「会话总结」，不展示病理诊断断言。使用低倍 → 10× → 40×，不沿用此前虚构的 20×。

## 影像与坐标

直接从原 SVS 提取，不能任意选美观的区域替代实际访问位置。

- 低倍 snapshot bbox: (56807,33807,16386,16386)，read_level=2。
- 10× snapshot bbox: (64952,33452,4096,4096)，read_level=1。
- 40× snapshot bbox: (67988,34288,1024,1024)，read_level=0。
- 用稍扩展的低倍底图 (55000,32000,20000,20000) 容纳三个真实视口。根据真实 bbox 算相机中心和缩放；中、高倍高分辨率图叠在底图的对应坐标，保持同一组织连续放大。
- 倍率采用记录中的近似值（约 2×、10×、40×）；比例尺依据 MPP 和当前视口像素范围计算。

## 首屏

保留「和 AI 一起，看看这张切片」。展示同一 TCGA 真实低倍截图，旁边列「低倍定位 → 放大查看 → 记录观察 → 整理结果」。不画白框，不加扫描线，不造细胞数量。

## 实现规格（自动循环 · 复核解读 · 双标注回看）

替换现有四步 + 播放按钮。进入可见区自动播放，隔约 3 秒循环；不要播放/暂停按钮。保留手动 +/- 缩放。`prefers-reduced-motion` 下跳到最后一拍（低倍双标注），不自动循环。图片未 decode 完成不开始。

### 流程（可比 15 秒长，动作必须清楚）

压缩为 8 拍。拍 5 把右上 10×→40× 收成一次连续放大。被守卫拦住的重复 goto 不入库。旁白是简化操作反馈，不是诊断句，也不引用内部推理。

| 拍 | 建议时长 | 镜头（level-0 中心 / 视口边长 / 倍率） | 画面与复核动画 | 状态文案（中文默认） |
| --- | --- | --- | --- | --- |
| 0 | 1.6 s | 全片中心，视口覆盖整片，≈0.5× | 三块组织同时可见 | 已打开切片，先看整张图再选区域。 |
| 1 | 1.8 s | (22000,28000) 视口 16386，≈2× | 移到左侧大块 | 低倍看到纤维脂肪背景和一处致密细胞团。 |
| 2 | 2.4 s | (25600,29350) 视口 1024，≈40× | 10×→40× 连续放大；出现复核卡 A | **解读结果显示：密集淋巴样细胞聚集。非目标区，不落标。** |
| 3 | 1.6 s | (55000,22000) 视口 16386，≈2× | 拉远平移到右上 | 换到下一块组织，低倍以脂肪和纤维为主。 |
| 4 | 2.0 s | (60500,27500) 视口 1024，≈40× | 连续放大右上边缘；不出现标注钉 | 高倍下细胞细节不清，未作明确标注。 |
| 5 | 1.6 s | (65000,42000) 视口 16386，≈2× | 平移到右下，弧形致密带 | 右下上方有一条弧形致密带，值得细看。 |
| 6 | 2.6 s | (68500,34800) 视口 1024，≈40× | 10×→40×；出现复核卡 B | **解读结果显示：该区域为目标区，标注待复核。** |
| 7 | 3.2 s 后停留 3 s 再循环 | 能同时框住两处标注的低倍（建议底图覆盖约 x 23000–70000、y 25000–37000） | 拉远；A、B 两钉同时清晰可见，带短标签 | 回看低倍：一处非目标不落标，一处目标区待复核。 |

拍 2 的钉中心 (25600,29100)，拍 6 的钉中心 (68000,34500)。最后一拍两钉都在画面内，标签不互相挡住。循环前停 3 秒。

### 复核标注动画

高倍到达后约 0.3 s，钉从标注点弹出（短缩放，无白框、无扫描线）。同时侧栏解读卡淡入，文案用上表加粗句。非目标钉用中性描边；目标钉用待复核强调色。拉远后钉留在世界坐标里，不跟着视口中心跑。不要显示「浸润性癌」等诊断词，用「密集淋巴样细胞聚集」「目标区，标注待复核」。

### 影像

现有 `tcga-session-base.jpg` 底图是 (55000,32000,20000,20000)，装不下左块 (25600,29100)。必须重切一张更宽的底图，至少覆盖两处标注加边距，例如 level-0 (22000,24000,50000,18000) 或能同时看见左块和右下的等价框。10× / 40× 细节图按真实 bbox 叠在同一坐标系：左块 40× (25088,29088,1024,1024)、右下 10× (64952,33452,4096,4096)、右下 40× (67988,34288,1024,1024)。可从 homePC `UPLOAD_DIR` 的 TCGA-49-AAR4 原 SVS 或 smokebot 副本提取。不要另选更好看的区域。MPP 0.2525，比例尺按当前视口计算。

### UI

- 去掉播放/暂停按钮和 `data-hp-play` / `data-hp-pause`。
- 保留比例尺、倍率、手动 +/-。
- 侧栏步骤改成与 8 拍对应，或改成「概览 → 左块复核 → 右上观察 → 右下复核 → 回看」。
- 顶栏阶段名跟随当前拍。
- 中英 i18n 成对；cache-bust `entry.css` / `entry.js` / `i18n.js`。
- 无内联脚本（CSP `script-src 'self'`）。
- 测试：`test_phase1_auth_ui.py` 若仍断言 play 按钮或旧四步文案，改成断言自动循环脚本、复核卡键、无 `data-hp-play`。锁定中文「直接体验 Demo」等入口文案不要改。

### 验收

可见区内自动开始，结束停 3 秒再循环。第一次复核出现「非目标区不落标」，第二次出现「目标区，标注待复核」。最后低倍两钉都在、标签可读。无播放按钮、无白框、无扫描线、无虚构计数、无诊断断言。中英切换不断动画。手机上镜头和标签仍可读。


进入可见区自动开始；离开或切后台暂停；用户手动暂停不被自动恢复；结束停留结果，可重播。减少动态效果偏好下不自动播放，手动播放用瞬时视口变换。图片未准备好时不开始，不把空白视口描述为已观察。

检查：真实坐标/高低倍对应；无拉伸；实际比例尺；无白框/扫描/虚构数量；中英同步；手机/桌面可读；公开图注明来源；不发布私人会话原文、内部推理或账号信息。
