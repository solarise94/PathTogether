# 复核总结：2026-09-21 三份配套文档(流量修复/注册授权/双协议)

复核人:主 agent(Astra 文档的 review 环节)。日期:2026-09-21。范围:仅文档,不涉及代码实现与部署。

## 一句话结论

三份文档的事实声明经两个仓库代码逐一核实,**全部成立**。三轮修订:v2 统一口径/精确落点/补测试入口/对齐措辞(8 处);v3 按用户决定把协议文稿改为非正式数据使用规则说明并移除全部【占位】(17 处);v4 按用户措辞简化注册页两块勾选文案、协议一补充"服务端保存切片数据、未授权不向第三方共享"承诺(4 处)。当前基线:注册方案 v3、协议文稿 **v4**、流量修复方案 v1。用户已授权实施与上线,由工作流 `dwfrun-9ed3eddb-6ee5-4aa5-9d75-4d6220c70e20` 执行(traffic/余额修复 + P0–P4 + homepc 部署)。

## 复核方式

1. 三个只读核查代理分头对照仓库现状验证文档声明(PathTogether 统计/余额、PathTogether 注册/协议、HistoPilot 恢复逻辑),逐项给出 CONFIRMED/WRONG 判定与 file:line 证据。
2. 主 agent 做跨文档一致性与需求覆盖审查,产出修订清单。
3. 修订由子代理实施,主 agent 重读改动区域 + grep 交叉检查验收;发现的问题发回子代理修复后复验(v2 一轮标点、v3 零返工)。

## 核实结果(全部通过,摘要)

**流量修复方案(v1,未改):**
- 调查报告 `investigation-20260921-traffic-balance-customer-errors.md` 存在,5/2/1 来源计数、382 次 login 污染、¥474.93→¥473.81、上传 42%、assistant 尾消息报错等关键数字均与方案一致。
- `site_stats_store.py` v3 规则集已在工作区(OkHttp 独立规则已移除,词尾泛化标记与方案描述一致);`app.py` 余额后台草实现(`_run_provider_balance_check_once` 等,含 advisory lock 724196021)、owner site-stats API 均存在。
- 管理插件 manifest 恰为 0.4.11,`source-policy.json` pin 与当前 manifest SHA256 同步;§6 列出的 9 个测试文件全部存在;工作区未提交改动(12 文件 +234 行)与 §0 描述吻合。

**注册/授权方案(v1→v3):**
- §2 十二项代码声明全部核实:public 枚举返回 `public_registration_not_supported`(app.py:3996)、四种注册模式(settings_store.py:48)、token 同事务消费(registration_store.py:996+)、`consent_version=research-data-20260916-v1`(test_application_store.py:17)、最新 migration 0059、无 /legal 路由、无 `REGISTRATION_ADMIN_EMAIL` 等。
- HistoPilot:`ensureContinuableTail` 在 src/agent-runner.ts:2520(private 方法)且 dist 含同名符号;两个 resume 测试文件存在;工作区干净(wip/ser8-dev)。

**双协议文稿(v1→v3):** 需求覆盖完整——必选/自愿、不预勾选、不影响正常使用、停留观察不记时长、旧选项不自动升级授权、每日 5 个自助账号、管理员通知,均有对应条款。

## 第一轮修订清单(v1→v2)

| # | 文件 | 位置 | 改动 |
| --- | --- | --- | --- |
| A1 | 注册方案 | 版本行 | v1→v2 及修订说明 |
| A2 | 注册方案 | §2 表格 | animation-finish 与标注入口精确标注为"目前在 app.js";viewer-core.js 无此两者、保持不内置采集 |
| A3 | 注册方案 | §4.1 | 前台文案与协议文稿注册提示统一为"每日最多 5 个新自助账号……以完成注册时的剩余名额为准",注明以协议文稿为准 |
| A4/A5 | 注册方案 | §11 | 回归命令补 `test_test_application_api.py`、`test_repair_invite_activated_applications.py`(旧测试申请路径) |
| B1 | 协议文稿 | 版本行 | v1→v2 及修订说明 |
| B2 | 协议文稿 | 协议一 §二 | 通知内容与实现方案 §4.5 对齐:注册时间、账号标识(ID/邮箱)、来源模式、当日序号;新增"不包含您是否同意数据共享的选择" |
| B3 | 协议文稿 | 协议一 §七 | 占位表格引导句改为"(发布说明:……)"格式,与文稿开头"括号中的发布说明不展示给普通用户"的约定一致 |

## 第二轮修订(v2→v3):协议改为非正式规则说明

用户决定:协议文稿不是正式法律文件,不填真实负责人/联系方式/服务商信息,以向用户说明数据使用规则为准。沿用同一流程(主 agent 起草细则 → 子代理实施 → 主 agent 全文复验 + grep 检查),共 17 处:

- 协议文稿(12 处):版本行 v3;开头说明改为"非正式法律文件、权利经自助入口行使";协议一/二头部四行去除占位——主体写为"HistoPilot / PathTogether 软件的开发与维护者(个人开发者)",生效日期"随对应功能上线生效",联系方式改为账号设置自助完成;§七 期限表填入实际规则(业务数据随账号存续、备份最长约 30 天轮换清除;运行记录按必要期限定期清理;授权/撤回证明最多 1 年);§七/§八 与协议二 §五 的"联系我们"改为自助入口表述;文末"发布配置附件"改为"发布说明"四条(非正式定位、版本/hash 受控、服务商按真实配置列明、研究期限须与实现一致)。
- 注册方案(5 处):版本行 v3;§3.2/§4.1 发布前置由"真实已发布协议/真实披露信息"改为"已发布的双协议文稿(规则说明性质,版本/hash 受控)";§9 改写为用户决定记录;§12"真实协议元数据"改为"协议版本/hash 元数据"。

## 验收检查(两轮复核后)

- 名额措辞两处逐字一致;通知"不含研究共享选择"双文档互洽;四个相对链接目标均存在;被改表格列数完整。
- v3:两文件【占位】计数为 0;三处版本行 v3 一致;Markdown 硬换行行尾双空格恰好 6 行(字节级复验);协议全文通读无悬空引用、无残留"联系我们"死链。
- 流量修复文档两轮均未触碰(保持 v1)。

## 第三轮修订(v3→v4):注册页勾选文案按用户措辞简化

用户给出确定措辞,4 处:
- 协议一 checkbox:`☐ 我已阅读并同意《用户协议与数据处理说明》` → `☐ 同意《用户协议与数据处理说明》`;辅助说明改为"服务器会保存您上传的切片等数据……除向您主动使用的模型服务商传输必要数据外,不会在您未授权的情况下向第三方共享这些数据"。
- 协议二 checkbox:`☐ 我自愿同意《数据共享与软件改进协议》……` → `☐ 向研究者共享使用数据用于优化本软件`;辅助说明改为"……agent 运行轨迹、用户对话、使用轨迹等信息……详见《数据共享与软件改进协议》"(去掉停留观察细述,链接指向正文)。
- 协议一 §四 数据处理表"切片、标注和会话"行补充服务端保存与未授权不向第三方共享的承诺。
- 版本号 v3→v4(标题行、协议一/二头部,共 3 处)。

## 遗留事项(留给后续)

1. ~~文档阶段遗留~~ 已进入实施:用户授权上线,工作流 `dwfrun-9ed3eddb-6ee5-4aa5-9d75-4d6220c70e20` 正在执行 traffic/余额修复(F1–F4)+ 注册/授权/研究遥测(P0–P4)+ HistoPilot 核验 + homepc 部署;实施与上线结果见后续 docs/implementation-release-20260921.md。
2. 实施时 migration 从 0060 开始;研究采集独立开关默认关闭;旧 consent_version=true 不自动升级。
3. 协议文稿现为非正式规则说明,无占位符;后续若转为正式法律文件,需补真实主体与联系方式并发布新文稿版本(文稿发布说明已注明)。
