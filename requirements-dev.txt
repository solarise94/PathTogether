# 开发/测试期依赖（不进运行镜像）。
# pgserver：内嵌真实 PostgreSQL 二进制，供 tests/test_pg_infra.py 起隔离实例跑
# 迁移与外键冒烟。本机无 postgres/podman/docker 时用它在进程内拉起 PG。
# 测试运行器必须显式声明，避免干净 CI 环境缺少 pytest。
pytest>=8.0
pgserver>=0.1.4
# 覆盖率（test-review P3-19）：CI json/PG 两个 job 均带 --cov；fail-under 阈值
# 经本机度量后设定（json 模式产品代码 TOTAL 54%，阈值 44 = 其下 10 点，宁低勿红）。
pytest-cov>=5.0
